/*import {
  buildAsyncState,
  buildAsyncReducers,
  buildAsyncActions,
} from '@thecodingmachine/redux-toolkit-wrapper'
import fetchFilteredService from '@/Services/ParkingSpots/FetchFiltered'

export default {
  initialState: buildAsyncState('fetchAll'),
  action: buildAsyncActions('parkingSpots/fetchFiltered', fetchFilteredService),
  reducers: buildAsyncReducers({
    errorKey: 'fetchAll.error', // Optionally, if you scoped variables, you can use a key with dot notation
    loadingKey: 'fetchAll.loading',
  }),
}
*/